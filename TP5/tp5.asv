clc;clear all;
%%%%%%%%%%%%%%
%% TP 5
 
%% Parametros configurables

pctTrain = 0.5; % el 50% es de entrenamiento el resto es de test
nTraing
%% Parametros del problema
a = 0.2;
m = 2;
g = 9.8;

datos_pendulo;
t   = datos(:,1);
q   = datos(:,2);
qd  = datos(:,3);
qdd = datos(:,4);
tau = datos(:,5);

%% Regresor

Y = tau - (a^2 *m*qdd+m*g*a*cos(q));
